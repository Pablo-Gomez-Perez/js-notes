
//alert("Hello, World!");

let a = 10, b = 40, c = 90;

console.table({a,b,c});

/**
 * Something really interesting about using var, is that the variable is stored in the global scope, it means that is stored in the window object,
 * and we can access it from anywhere in our code, but the import order of the js file is important.
 */
var myName = "Pablo Gomez";

//var outerWidth = 12345;


